services:
  - type: web
    name: snapsave
    runtime: python
    repo: https://github.com/YOUR_USERNAME/snapsave  # Badme change karna
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0